This directory contains the files to compute the NTK for a shallow network

* **shallow.py**
Contains the implementation of the network and a class to compute its NTK.
There are also unit tests to test the NTK computation.
* **data/**
The directory is used to store the CIFAR dataset
* **shallowplot.py**
Contains the code to generate the figure plotting the NTK spectrum
